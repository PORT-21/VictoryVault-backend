post:
  operationId: change_password
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: string
          name: new_password
          example: NewTestPassword
  responses:
    '204':
      description: пароль успешно изменён
